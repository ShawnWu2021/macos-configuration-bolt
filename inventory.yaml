groups:
- name: nodes
  targets:
    - uri: 192.168.56.2
      alias: mac1
    - uri: 192.168.56.3
      alias: mac2
  config:
    transport: ssh
    ssh:
      user: vagrant
      password: vagrant
      host-key-check: false

